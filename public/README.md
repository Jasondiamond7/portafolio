# public/

Drop these files here (referenced by `index.html` and `src/content/profile.ts`):

- `resume.pdf` — your CV, linked from the hero "Download CV" button.
- `og-image.png` — 1200×630 social preview image.
- `favicon.svg` — replace the Vite default.
